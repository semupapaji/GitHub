{
  "builds": [
    {
      "src": "app.py",           // 👈 Your main Python file
      "use": "@vercel/python"    // 👈 Vercel's Python runtime
    }
  ],
  "routes": [
    {
      "src": "/(.*)",            // 👈 Match ALL URLs
      "dest": "app.py"           // 👈 Send to app.py
    }
  ]
}
